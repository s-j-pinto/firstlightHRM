
name: 4-Hour SMS Follow-up Cron Job

on:
  schedule:
    # Runs every 4 hours.
    # See https://crontab.guru/ for cron syntax.
    - cron: '0 */4 * * *'
  workflow_dispatch: # Allows manual triggering from the Actions tab in GitHub for testing

jobs:
  call_sms_follow_up_webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Call SMS follow-up webhook
        run: |
          curl --request GET \
          --url https://care-connect-360--firstlighthomecare-hrm.us-central1.hosted.app/api/cron/sms-follow-up \
          --header 'Authorization: Bearer ${{ secrets.CRON_SECRET }}' \
          --fail
