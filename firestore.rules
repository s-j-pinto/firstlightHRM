rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for any signed-in user on any document.
    // This is a broad rule suitable for development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
